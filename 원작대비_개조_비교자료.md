# Connect AI — 원작 대비 투자 AI 개조 비교 자료

> **목적**: 원작자님께 저희가 수정한 시스템의 검토를 의뢰드리기 위한 자료입니다.
> 원작의 설계를 최대한 존중하면서, 저희 사용 환경(로컬 소형 모델 + 미국 주식 투자)에
> 맞춰 어디를 왜 어떻게 바꿨는지 한눈에 보이도록 정리했습니다.
>
> 작성 기준: 실제 git 커밋 이력 + 소스 코드. (추정·각색 없음)

---

## 0. 한 줄 요약

원작은 **"LLM이 스스로 판단하는 자율 지식 에이전트(1인 기업 OS)"** 입니다.
저희는 이것을 **"코드가 데이터를 강제 주입하고 LLM은 분석만 하는 미국 주식 투자 어시스턴트"** 로
개조했습니다. 가장 큰 변화는 **신뢰성 확보 방식**입니다 — 대형 모델 전제의 자율성을,
소형 로컬 모델(9B)에서도 날조 없이 동작하도록 **결정적(deterministic) 파이프라인**으로 바꿨습니다.

---

## 1. 출발점 — 무엇을 포크했나

| 구분 | 원작 (포크 시점) |
|------|------------------|
| 버전 | v2.89.122 (1인 기업 OS) |
| 컨셉 | **P-Reinforce 아키텍처** — 자율 지식 정원사(Autonomous Gardener) |
| 대상 사용자 | 유튜버·콘텐츠 크리에이터 (1인 기업가) |
| 에이전트팀 | 비서·유튜브·작가·디자이너·인스타그램·비즈니스·개발자·리서처 (CEO + 8) |
| 핵심 기능 | 자율 지식 구조화, A.U 연동, Auto-Git Sync, 모델 자동 감지 |

> 저희가 포크한 직후가 **v2.90.0**, 현재가 **v3.1.2** 입니다.
> (약 30개 마이너 버전에 걸친 점진적 개조)

---

## 2. 가장 큰 구조적 변화 — "자율 판단" → "강제 주입"

이 부분이 원작자님께서 가장 흥미롭게 보실 핵심입니다.

### 원작 방식 (P-Reinforce / Zero-Interaction)
```
사용자 입력 → LLM이 스스로 의미 판단 → LLM이 도구·파일 작업을 자발적으로 결정·실행
            → 10_Wiki / 00_Raw / Skills 로 자율 분할 저장
```
- **전제**: 충분히 똑똑한 모델(대형 LLM)이 스스로 옳은 도구를 고르고 규격을 지킨다.
- **장점**: 범용적, 사용자가 시키지 않아도 알아서 정리.

### 저희가 겪은 문제
저희 환경은 **LM Studio + Qwen 계열 9B (Q4) 로컬 모델** 입니다. 여기서:
- 9B 모델이 도구를 **자발적으로 호출하지 않거나** 엉뚱한 명령을 고름
- 호출해도 followUp 루프가 멈추거나, 데이터 없이 **숫자를 지어냄(환각)**
- 계산(밸류에이션·지표·포지션 사이징)을 **산수로 틀림**
- 한국어 응답에 **중국어·간체자 혼입**

### 저희 방식 (forcedArgs — 결정적 도구 라우팅)
```
사용자 입력 → [코드]가 키워드 감지 → [코드]가 Python 도구를 먼저 실행
            → 실데이터(JSON)를 컨텍스트에 주입 → LLM은 "읽고 분석만"
```
- LLM에게 **판단의 자유를 주지 않습니다.** 데이터 수집·계산은 전부 Python이 끝냅니다.
- 모델은 이미 주어진 실데이터를 해석·요약하는 역할만 합니다.
- 결과: **날조 원천 차단, 산수 오류 차단, 소형 모델에서도 안정적 동작.**

> 즉, 원작의 "똑똑한 모델을 믿는 자율성"을, 저희는 "모델을 덜 믿고 코드로 보증하는 결정성"으로
> 바꾼 것입니다. 트레이드오프(범용성↓ vs 신뢰성↑)를 의도적으로 선택했습니다.

---

## 3. 알고리즘 레벨 비교

### (A) 지식 적재 — 원작 vs 현재

| 항목 | 원작 (Zero-Interaction Styling) | 현재 |
|------|-------------------------------|------|
| 트리거 | LLM이 자율 판단 | 코드의 키워드 감지(예: "종합 분석", "포트폴리오") |
| 데이터 출처 | 사용자가 던진 Raw Data | yfinance·SEC EDGAR 실시간 API |
| 구조화 주체 | LLM(`<create_file>` 자율) | Python 도구가 JSON 생성, LLM은 해석만 |
| 환각 방지 | 모델 품질에 의존 | sanity check + "JSON에 없는 값 금지" 강제 지침 |

> 원작의 자율 지식 구조화 엔진(`/api/brain-inject`, 00_Raw → 10_Wiki 분할)은 **코드상 그대로 보존**되어
> 있습니다. 다만 소형 모델에서는 자율 분할이 불안정해, 투자 흐름은 forcedArgs 경로로 우회했습니다.

### (B) 신규 도입한 결정적 계산 도구 (원작에 없던 부분)

투자 정확성을 위해 **모든 수치 계산을 Python으로 이관**했습니다. (LLM 산수 배제)

| 도구 | 역할 | 도입 버전 |
|------|------|-----------|
| `stock.py` | 시세·밸류에이션·재무·목표가 + 기술지표(RSI/MA/MACD/ATR) + 포지션 사이징 | v2.90.0~ |
| `macro.py` | 거시·센티먼트 스냅샷(VIX·금리·달러·지수) | v2.92.0 |
| `backtest.py` | MA크로스·RSI 전략 백테스트 | v2.92.0+ |
| `sec.py` | SEC EDGAR 공식 공시·재무 (urllib, 키 불필요) | v2.93.0 |
| `portfolio.py` | 보유종목 손익·손절거리·매매 액션 | v2.94.0 |
| `screen.py` | 관심종목/테마 저평가·모멘텀 랭킹 | v2.94.0 |

### (C) 데이터 품질 검증 (최근 v3.1.2)

저장·재인용 시 오염을 막기 위해, yfinance가 내보내는 비현실적 값을 **입력 단계에서 차단**합니다.
```
예) 적자기업에 profitMargin = 174.88% (yfinance 오류값)
    → None 처리 + data_warnings 기록 → 모델이 복원·추정 못 함
```
검증 필드: profitMargin / ROE / revenueGrowth / dividendYield / beta + 애널리스트 정합성.

---

## 4. 에이전트팀 재편

| 원작 (콘텐츠 크리에이터) | → | 현재 (투자 전문가팀) |
|------------------------|---|---------------------|
| 비서 (Secretary) | | (유지 — 일정·브리핑) |
| 유튜브 / 작가 / 디자이너 / 인스타그램 | | 기술분석가 · 펀더멘털분석가 · 리스크매니저 |
| 비즈니스 | | 매크로분석가 · 센티먼트분석가 |
| 개발자 | | 퀀트엔지니어 |
| 리서처 | | 리서처 (SEC·발굴) · 포트폴리오매니저 |
| (CEO 총괄) | | **CIO** (종합 분석 통합) |

> 페르소나·시스템 정체성(system identity)을 미국 주식 투자팀으로 재작성했습니다.
> "CIO 종합 분석" 모드는 4개 도구(펀더멘털+기술+리스크+거시)를 한 번에 사전 실행해
> 한 컨텍스트로 통합 브리핑합니다.

---

## 5. 보존한 원작 자산 (그대로 살아있는 부분)

저희가 **건드리지 않고 유지한** 원작의 핵심 인프라:
- ☁️ **Auto-Git Sync** — 지식 생성 시 자동 commit/push (그대로 활용)
- 🧠 **Second Brain (00_Raw / 10_Wiki)** — 폴더 구조·자동 스캔·컨텍스트 주입
- 🔗 **모델 자동 감지** (Ollama/LM Studio `v1/models`)
- 🤖 **24시간 자율 사이클** (autoCycle, 15분 디스패치, 데일리 브리핑) — 투자용으로 재활용 예정
- 🔌 **`/api/brain-inject` 브릿지** — 외부 지식 주입 엔드포인트

---

## 6. 원작자님께 검토 요청드리고 싶은 점

1. **forcedArgs 우회 접근**이 원작 P-Reinforce 철학과 충돌하는지, 아니면 "소형 모델용 보완 레이어"로
   공존 가능한 설계인지에 대한 의견.
2. 저희가 자율 지식 구조화 엔진을 **비활성화가 아니라 우회**해 둔 상태인데, 이를 투자 도메인에서도
   안정적으로 되살릴 방법(예: 구조화 규격을 더 단순화)에 대한 조언.
3. 24시간 자율 사이클을 투자 감시(보유종목 알림·발굴 스캔)로 확장할 때, 원작 디스패치 구조를
   그대로 써도 되는지에 대한 검토.
4. 전반적인 코드 품질·안전장치(특히 데이터 검증·환각 차단)에 대한 피드백.

---

## 부록 — 주요 개조 커밋 (시간순)

```
v2.90.0  투자팀으로 에이전트 재편 + 시스템 정체성 재작성 + yfinance 전환
v2.92.0  macro.py (거시·센티먼트)
v2.92.0+ backtest.py (퀀트)
v2.93.0  sec.py (SEC EDGAR 공식 데이터)
v2.94.0  portfolio.py + screen.py (보유관리·발굴)
v2.97.1  매수비율·추세방향 Python 사전계산 (9B 산수오류 차단)
v3.0.0   결정적 투자 도구 라우팅 (forcedArgs) — 9B 명령선택 우회 ★핵심
v3.0.5   종합 분석 멀티 프리페치 (CIO 통합)
v3.0.8   투자 면책 자동 주입 (가드레일)
v3.0.9   포트폴리오 관리 통합 흐름
v3.1.0   종목 발굴 + 검증 통합 흐름
v3.1.2   데이터 sanity check 확대 (저장 기반 다지기)
```
